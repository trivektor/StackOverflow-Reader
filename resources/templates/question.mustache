<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' type='text/css' href='http://getbootstrap.com/dist/css/bootstrap.min.css'>
    <style>
      main {
        padding: 10px;
        display: block;
      }

      section {
        margin-bottom: 20px;

        .lead {
          font-size: 40px;
          margin-top: 0;
        }
      }

      .score {
        font-size: 24px; font-weight: 100; float: left; width: 40px; text-align: center
      }

      li {
        padding: 10px 0;
        border-top: 1px solid #eee;
      }

      .tags {
        display: block; padding: 10px 0; border-top: 1px solid #eee
      }

      .answer .details,
      .question-text {
        padding-left: 50px
      }
    </style>
  </head>
  <body>
    <main>
      <section>
        <h1 class='lead'>{{question.title}}</h1>
        <div class='clearfix'>
          <span class='score'>{{question.score}}</span>
          <div class='question-text'>{{question.body}}</div>
        </div>
      </section>
      <section>
        <h2 class='lead'>Answers: {{question.answer_count}}</h2>
        <aside class='tags'>
          {{#question.tags}}
            <label class='label label-default'>{{.}}</label>
          {{/question.tags}}
        </aside>
        <ul class='list-unstyled answers-list'>
          {{#answers}}
            <li class='answer'>
              <figure class='score'>{{score}}</figure>
              <div class='details'>{{body}}</div>
            </li>
          {{/answers}}
        </ul>
      </section>
    </main>
  </body>
</html>